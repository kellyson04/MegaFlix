spring:
  application:
    name: MegaFlix

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/megaflix_db
    username: root
    password: teste123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  security:
    user:
      name: admin
      password: admin123

server:
  port: 8080